
# 数据库配置
# .env.production
DB_HOST=mysql #注意：在Docker容易中，这里应该是服务名而不是localhost
DB_PORT=3307
DB_USERNAME=app_user
DB_PASSWORD=userpassword
DB_DATABASE=user_db
DB_SYNCHRONIZE=false  # 开发环境为true，生产环境必须为false
# 应用配置
APP_PORT=3000
NODE_ENV=production